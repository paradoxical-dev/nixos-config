# Hosts

This direcotry contains each of the hosts which are buildable from this config.

To create a new host, simply copy the [TEMPLATE](./TEMPLATE) sub directory within this direcotry and rename it to the desired hostname of the new host.

For more configureation options, see the [modules](../modules/) directory
