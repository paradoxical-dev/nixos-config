# TEMPLATE

A base template for a new host. Contains the `configuration.nix`, `home.nix`, and `default.nix` files needed for configuring a new host.


> [!NOTE]
> While the `default.nix` file can stay unchanged, both the `configuration.nix` and `home.nix` files will need to be updated to reflect the hosts configuration
