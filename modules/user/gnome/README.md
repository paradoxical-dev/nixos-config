# GNOME

Different from the system [GNOME module](../../system/gnome), this module describes how home-manager uses the dconf package.

It is responsible for saving both GNOME system settings as well as extensions, and configures GTK.

## Options

### `gnome.enable`

Enables the home-manager configuration of dconf settings and GTK

**Type**: `bool`

**Default**: `false`
